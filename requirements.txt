# alt_celery2 传统依赖清单（与 pyproject.toml 保持同步）
# Python >= 3.13

# --- 核心依赖 ---
# Celery 5.5 起官方支持 Python 3.13
celery>=5.5.0,<6.0.0
# Redis 客户端（Celery broker/backend 均基于 Redis 协议）
redis>=5.2.0,<7.0.0

# --- 监控 ---
# Flower 2.x：Web 监控面板，兼容 Celery 5
flower>=2.0.1,<3.0.0
